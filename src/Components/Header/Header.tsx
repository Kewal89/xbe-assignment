import { Box } from "@mui/material"
import styles from "./Header.module.css"

const Header = () => {
  return <Box className={styles.HeaderRoot}>
    <Box>
      Crew Wages Calculator
    </Box>
  </Box>
}

export default Header
